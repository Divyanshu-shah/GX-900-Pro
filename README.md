This is the official website for the **GX-900 Pro** — next-generation immersive audio engineered for competitive gaming.

## Getting Started

First, run the development server:

```bash
npm run dev
# or
================
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `src/app/page.tsx`. The page auto-updates as you edit the file.

## Features

- 🎧 **Immersive Hero Section:** Animated video background, bold product branding, and tagline “Enter the game.”
- 🛠️ **Engineering:** Precision 50mm neodymium drivers, ergonomic design for long sessions, advanced cooling system.
- 🔊 **Audio System:** 360° spatial audio, 7.1 virtual surround, AI-powered noise-cancelling mic, animated sound waves.
- ⚡ **Performance:** Ultra-low latency 2.4GHz wireless, 60h battery, USB-C fast charge, 7.1 surround channels.
- 📊 **Technical Specs:** Detailed breakdown for audio, microphone, connectivity, and design.
- ✨ **Modern UI:** Parallax, 3D effects, animated gradients, custom cursor, and smooth transitions.

## Learn More

This project is built with [Next.js](https://nextjs.org/) and uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font/geist), a new font family for Vercel.

To learn more about Next.js, check out:

- [Next.js Documentation](https://nextjs.org/docs) – learn about Next.js features and API.
- [Learn Next.js](https://nextjs.org/learn) – an interactive Next.js tutorial.
- [Next.js GitHub repository](https://github.com/vercel/next.js/) – your feedback and contributions are welcome!

## Deploy on Vercel

The easiest way to deploy your Next.js app is to use the [Vercel Platform](https://vercel.com/new) from the creators of Next.js.

Check out [Next.js deployment documentation](https://nextjs.org/docs/deployment) for more details.
To get started with the NextGen React App, follow these steps:

1. **Setup**: Clone the repository and navigate to the project folder in your terminal.
2. **Environment**: Install the required dependencies by running `npm install` or `yarn install`.
3. **Build**: Run `npm run build` or `yarn build` to build the project.
4. **Deploy**: Run `npm run start` or `yarn start` to start the development server.

Here is an example of the `next.config.ts` file:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // config options here
};

export default nextConfig;
```
This file defines the project's Next.js configuration, including settings for optimization, internationalization, and more.

🧪 Testing Instructions
======================
To run the project's tests, follow these steps:

1. **Install dependencies**: Install the required testing dependencies by running `npm install` or `yarn install`.
2. **Run tests**: Run `npm run test` or `yarn test` to run the project's tests.

The project uses Jest and Cypress for testing, and includes a comprehensive set of tests for each component and feature.
